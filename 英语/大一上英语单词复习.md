- Unit4
	`mock` 取笑;嘲笑 `sin` 原罪 `look the part` 挺像这么回事 `sit up` 做起来 `turn loose` 放手;放纵
	`acute` 急性的 `nonsense` 废话;荒唐的念头;愚蠢的行为 `stalk` 可怕地蔓延/游荡;潜行;偷偷接近 `subtact` 减去,扣除 `cling` 紧紧抓住/抱住;坚持;不愿放弃 `flutter` 飘动,晃动 `merry` 高兴的,愉快的
	`mingle` 混合 `scatter` 散布 `for the rest` 至于其他的 `in tune` 和谐的 `to`