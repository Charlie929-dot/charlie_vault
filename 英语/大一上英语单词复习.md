- Unit4
	`mock` 取笑;嘲笑 `sin` 原罪 `look the part` 挺像这么回事 `sit up` 做起来 `turn loose` 放手;放纵
	`acute` 急性的 `nonsense` 废话;荒唐的念头;愚蠢的行为 `stalk` 可怕地蔓延/游荡;潜行;偷偷接近 <mark style="background: #FF5582A6;">`subtract` 减去,扣除</mark> `cling` 紧紧抓住/抱住;坚持;不愿放弃 `flutter` 飘动,晃动 `merry` 高兴的,愉快的
	`mingle` 混合 `scatter` 散布 `for the rest` 至于其他的 `in tune` 和谐的 `to excess` 过度
- Unit3
	`correspond` 通信 `split` 裂开;破裂 `thrust` 挤入;插入;==猛推== `come alive` 变得生机勃勃 
	`hold (sth.) out` 递出 `take a chance (on sth.)` 碰运气;冒险 `delicate` 精致的;易碎的 
	`fertile` 富饶的;肥沃的 `bud` 发芽,萌芽;芽,花蕾 `glow` 光亮,光辉 `haunt` (鬼魂般)游荡;==使==担忧,==使==苦恼 `margin` 页边空白;边缘 `go one's way` 和某人同路
- Unit2
	`awful` 非常;极大;可怕;糟糕 `keep up correspandence` 保持联系 `by heart` 熟记 
	`every now and then` 偶尔,间或 `or something` 诸如此类 `apologetically` 带有歉意地 
	`choke up` 堵塞,(因感动)哽咽 `not much of a` 不太好的
- Unit1
	`intangible` 无形的；无实物的 `undergraduate` 本科生 `emulate` 仿效 `stray` 偏离 
	`in the first place` 首先 `take charge` 负起责任;掌握