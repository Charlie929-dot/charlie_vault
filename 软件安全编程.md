# Q
> 使用 nano 和 vim 编辑器？


没问题！为了让你以后操作更清晰，我把你从“制作环境”到“写代码运行”的完整流程，按照**Windows PowerShell**和**Docker容器**两个阶段进行了梳理。

你可以把这份总结当作你的“通关秘籍”：

### 🖥️ 第一阶段：在 Windows PowerShell 中操作

**1. 构建镜像（制作环境模具）**
*只需在第一次或修改了 Dockerfile 时执行一次。*
```powershell
docker build -t secprog .
```

**2. 启动容器（进入工作环境）**
*每次开始写代码前执行。建议把这条命令保存在记事本里，直接复制粘贴。*
```powershell
docker run -it --rm --cap-add=SYS_PTRACE --security-opt seccomp=unconfined -v "${PWD}":/home/student/work secprog
```
> 实际可用
```
docker run -it --rm `
--cap-add=SYS_PTRACE --security-opt seccomp=unconfined `
-v "C:/Users/HP/Desktop/软件安全编程:/home/student/work" `
secprog
```
---

### 🐧 第二阶段：在 Docker 容器内部操作

*成功进入容器后，你的提示符会变成 `student@...$`，接下来都在这里面操作。*

**3. 安装必备工具（按需执行）**
*如果缺少文档工具，可以运行以下命令补全环境：*
```bash
sudo apt update
sudo apt install man manpages-dev
```

**4. 编写 C 语言代码**
*使用你熟悉的编辑器（如 nano 或 vim）创建代码文件：*
```bash
nano hello.c
```
*(编写完成后，按 `Ctrl+O` 保存，按 `Enter` 确认，再按 `Ctrl+X` 退出)*

**5. 编译代码**
*将 C 语言代码编译成可执行文件：*
```bash
gcc hello.c -o hello
```

**6. 运行程序**
*注意：在 Linux 中运行当前目录的程序，必须加 `./`：*
```bash
./hello
```

**7. 退出容器**
*工作结束，准备离开：*
```bash
exit
```

---

**💡 核心小贴士：**
*   **代码在哪里？** 你写的 `hello.c` 保存在你电脑原本的文件夹里，Docker 容器只是个“临时工作间”，随时可以安全退出。
*   **命令记不住怎么办？** 在 PowerShell 里按 **向上箭头 (↑)** 可以找回之前敲过的长命令，不用每次都重新手打！

---
要不要我把常用的调试命令（比如 gdb）也整理一份？后面实验肯定会用到。

# 1
pwd 
> show working path
# 2
ls 
> show files in current folder
# 3
0x0000555555555195 in main () at gdb_example.c:11
11                      if(!(num % i))
> 程序暂停在了11行

# 4
无法选中powershell中的内容而是报告鼠标点击和拖拽事件
```
vim
:q! #退出vim
```