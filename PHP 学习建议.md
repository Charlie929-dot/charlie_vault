### 🔴 第一梯队：核心必学（直接决定你能不能挖洞）

这些内容在 CTF 和实战中天天见，必须学透：

1. **PHP 超全局变量**：`$_GET`、`$_POST`、`$_REQUEST`、`$_COOKIE`、`$_SESSION`。这是所有 Web 漏洞的入口，必须懂它们是怎么接收数据的。
2. **PHP 表单处理 / 表单验证**：理解用户输入是如何进入后端的。
3. **PHP 文件处理**：`文件打开/读取`、`文件创建/写入`、`文件上传`。这是 CTF 中**文件包含漏洞**、**任意文件读取**、**文件上传漏洞**的底层逻辑。
4. **PHP 包含文件**：`include` 和 `require`。CTF 高频考点，必须懂。
5. **PHP OOP（面向对象）**：`类/对象`、`构造函数`、`魔术方法`（虽然目录里没明确写，但 OOP 里必有）。这是 PHP 安全中最难、含金量最高的**反序列化漏洞**的基础。
6. **PHP 过滤器**：理解 PHP 内置的过滤机制，以及黑客是如何绕过这些过滤的。
7. **PHP 正则表达式**：CTF 中经常考正则匹配绕过（比如怎么绕过 `preg_match` 的限制）。

### 🟡 第二梯队：基础语法（能看懂就行，不用深究）

这部分你只需要花几天时间快速过一遍，知道它们长什么样，能看懂别人写的代码在干嘛即可：

- PHP 变量、数据类型、字符串、数组、运算符、流程控制（if/for/foreach）、函数。
- _建议：直接找一段有漏洞的 PHP 源码，边看边查这些语法，比干看教程快十倍。_