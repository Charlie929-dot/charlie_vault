# 755
### 题目
![[755.docx]]
### 解答
```c++
#include <iostream>
#include <cstring> // 用于字符串操作，如 strcmp 和 strcpy

// 使用标准命名空间，简化代码
using namespace std;

// 1. 定义学生信息的结构体
// 包含姓名、学号、性别，以及一个指向下一个学生的指针
struct Student {
    char name[21]; // 姓名，最多20个字符，加1个结束符'\0'
    int id;        // 学号
    char gender;   // 性别
    Student* next; // 指向下一个节点的指针
};

int main() {
    // 初始化链表头指针和尾指针为空
    Student* head = NULL;
    Student* tail = NULL;

    char tempName[21];

    // 2. 循环读取学生信息，创建链表
    // 每次读取一个名字，检查是否为 "#"
    while (cin >> tempName) {
        // 如果名字是 "#"，则结束输入
        if (strcmp(tempName, "#") == 0) {
            break;
        }

        // 创建一个新的学生节点
        Student* newNode = new Student();

        // 将刚才读取的名字复制到新节点中
        strcpy(newNode->name, tempName);
        // 读取该学生的学号和性别
        cin >> newNode->id >> newNode->gender;
        // 新节点是当前链表的最后一个，所以它的next指针为空
        newNode->next = NULL;

        // 将新节点添加到链表尾部
        if (head == NULL) {
            // 如果链表是空的，新节点既是头也是尾
            head = newNode;
            tail = newNode;
        } else {
            // 如果链表不为空，将当前尾节点的next指向新节点
            tail->next = newNode;
            // 更新尾指针为新节点
            tail = newNode;
        }
    }

    // 3. 读取要删除的学生的学号
    int deleteId;
    cin >> deleteId;

    bool found = false; // 标记是否找到了要删除的学生
    Student* current = head;
    Student* prev = NULL; // 指向当前节点的前一个节点

    // 4. 查找并删除学生节点
    while (current != NULL) {
        if (current->id == deleteId) {
            found = true; // 找到了

            if (prev == NULL) {
                // 情况一：要删除的是头节点
                head = current->next;
            } else {
                // 情况二：要删除的是中间或尾部节点
                // 将前一个节点的next指针绕过当前节点，指向下一个节点
                prev->next = current->next;
            }
            delete current; // 释放被删除节点的内存
            break; // 找到并删除后，退出循环
        }

        // 如果当前节点不是要删除的，则继续向后遍历
        prev = current;
        current = current->next;
    }

    // 5. 根据查找结果进行输出
    if (found) {
        // 如果找到了并删除了，遍历输出剩余的学生信息
        current = head;
        while (current != NULL) {
            cout << current->name << " " << current->id << " " << current->gender << endl;
            current = current->next;
        }
    } else {
        // 如果没有找到，输出 "No"
        cout << "No" << endl;
    }

    // 6. 释放链表中所有剩余节点的内存（好习惯）
    current = head;
    while (current != NULL) {
        Student* temp = current;
        current = current->next;
        delete temp;
    }

    return 0;
}
```
> [!note] 
> |特性|free|delete|
|:--|:--|:--|
|本质|C 语言标准库函数 (`<stdlib.h>`)|C++ 运算符|
|配对使用|配合 `malloc` / `calloc`|配合 `new`|
|析构函数|不调用 (对象资源可能泄漏)|调用 (自动清理资源)|
|类型安全|无 (接收 `void*`)|有 (检查指针类型)|
|数组处理|不区分，统一 `free(ptr)`|必须用 `delete[]`|
|内存碎片|容易产生碎片|相对更优 (配合编译器优化)|
# 756
### 题目
[[59b2659183ffaff0182de81840b6963d_MD5.docx|Open: 756.docx]]
![[59b2659183ffaff0182de81840b6963d_MD5.docx]]
### 解答
```c++
#include<iostream>

using namespace std;

struct node{
	int h;
	node *next;
};

int main(){
	node *head = NULL;
	node *tail = NULL;
	node *insert =new node();
	int N=0;
	cin>>N>>insert->h;
	
	while(N--){
		node *s = new node();
		cin>>s->h;
		if(!head){
			head=s;
			tail=head;
		}
		else{
			tail->next = s;
			tail = tail->next;
		}
	}
	node *cur = head;
	
    // --- 高级插入逻辑 ---
    node **p = &head; // 定义一个二级指针，指向头指针的地址
    
    // 寻找插入位置：只要当前指针指向的节点存在，且值比插入值小，就继续往后找
    while (*p && insert->h > (*p)->h) {
        p = &(*p)->next; // p 指向下一个节点的 next 成员的地址
    }
    
    // 执行插入：直接修改 *p 指向的地方
    insert->next = *p;
    *p = insert;
    // ------------------
	//输出
	cur = head;
	while(cur){
		cout<<cur->h<<" ";
		cur = cur->next;
	}
	return 0;
}

//每次都要分配内存，否则没办法赋值给NULL
```
> [!note] 
> 1. `new node()`: 分配内存, 并初始化
> `new node`: 分配内存, 不初始化, 数据为垃圾值
> 2. 空指针不可访问, 会报错

### 链表创建
```c++
	while(N--){
		node *s = new node();
		cin>>s->h;
		if(!head){
			head=s;
			tail=head;
		}
		else{
			tail->next = s;
			tail = tail->next;
		}
	}
```