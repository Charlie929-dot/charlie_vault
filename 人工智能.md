# AI技术回顾
## 分类
`任务`
### 生成式
### 判别式
`学习方式`
### 有监督
### 无监督
### 强化式
> agent 执行一步，环境 反馈一步

## 人工神经元模型

```mermaid
graph LR
    subgraph 输入层
        X1[x₁]
        Xn[xₙ]
        Xθ[-1]
    end
    subgraph 加权层
        W1[w₁]
        Wn[wₙ]
        Wθ[θ]
    end
    subgraph 计算层
        Sum[∑ 求和]
        F[f(·) 非线性函数]
    end
    Y[y 输出]

    X1 --> W1 --> Sum
    Xn --> Wn --> Sum
    Xθ --> Wθ --> Sum
    Sum --> V[V] --> F --> Y

    style Sum fill:#d62728,color:#fff
    style F fill:#1f77b4,color:#fff
    style V fill:#f0f0f0,stroke:#333,stroke-width:1px

```

## 卷积神经网络
`目的&意义`
### 卷积层
> 滑窗卷积运算

![[IMG_20251218_142524.jpg]]
1. 模板覆盖原图
2. 对应位置取乘积求和
- 填充（扩展长度）->输入输出大小保持一样
- 步幅（模板移动步长）->决定特征图大小
### 池化层->降维
#### 池化运算
> 局部平移不变性

##### 最大池化
> 模板覆盖取最大值
##### 平均池化
> 模板覆盖取平均值




