---
aliases:
  - C语言结构体超详解（小白一看就懂，多维度分析！！！！）
tags:
  - "#C语言"
  - "#CSDN"
---
## 📝 基本信息

- **原文链接**: [CSDN博客文章](https://blog.csdn.net/weixin_45031801/article/details/127621419)
- **分类标签**: C语言, 结构体, 编程基础
- **作者**: CSDN博主
- **适用人群**: C语言初学者, 需要巩固结构体知识的学习者

## 🎯 快速了解

这篇博客全面系统地讲解了C语言中结构体的各种知识点，从基础概念到高级用法，内容丰富详实。主要亮点包括：

- 结构体的定义与使用
- 结构体指针的多种应用场景
- typedef关键字与结构体的结合使用
- 结构体数组和指针数组
- 实际编程题目练习

文章采用循序渐进的方式，配合大量代码示例，非常适合初学者系统学习结构体知识。

## 🗺️ 知识图谱

## 📚 内容解读

### 一、结构体基础

1. **为什么需要结构体**  
    结构体可以将不同类型的变量组合成一个整体，便于管理复杂数据（如学生信息包含姓名、学号、成绩等）。
    
2. **结构体定义**
    
    ```
    struct tag {
        member1;
        member2;
    } variable-list;
    ```
    
    - `struct`是关键字
    - `tag`是结构体标签名
    - `member1`, `member2`是成员变量
    - `variable-list`是结构体变量
3. **三种定义方式**
    
    - 先定义类型再定义变量
    - 定义类型同时定义变量
    - 直接定义变量（匿名结构体）

### 二、结构体使用

1. **初始化**  
    可以在定义时直接初始化：
    
    ```
    struct books {
        char title[50];
        char author[50];
    } book = {"C语言", "xingaosheng"};
    ```
    
2. **成员访问**  
    使用`.`运算符访问成员：
    
    ```
    printf("书名: %s", book.title);
    ```
    
3. **结构体数组**  
    数组元素为结构体：
    
    ```
    struct stu {
        char name[20];
        int age;
    } class[5];
    ```
    

### 三、结构体指针（重点）

1. **指向结构体变量**
    
    ```
    struct stu *p = &stu1;
    printf("%s", p->name);  // 使用->访问成员
    ```
    
2. **指向结构体数组**
    
    ```
    struct stu stus[5], *ps;
    for(ps=stus; ps<stus+5; ps++) {
        printf("%s", ps->name);
    }
    ```
    
3. **成员为指针**  
    需要特别注意内存分配：
    
    ```
    struct Student {
        char *Name;
        int number;
    };
    stu.Name = (char*)malloc(sizeof(char));
    ```
    

### 四、typedef与结构体

1. **定义结构体别名**
    
    ```
    typedef struct TreeNode {
        int Element;
        struct TreeNode *Left;
    } Node;  // Node是struct TreeNode的别名
    ```
    
2. **定义结构体指针**
    
    ```
    typedef struct TreeNode *TreePtr;
    TreePtr p;  // 等价于struct TreeNode *p
    ```
    

### 五、实战练习

1. **书籍排序**  
    使用qsort对结构体数组按价格排序
```
#include <stdio.h>
#include <stdlib.h>
typedef struct Book 
{
    char name[100];
    int price;
}BOOK;
int cmp_by_price(const void* e1, const void* e2)  // psort()排序
{
    BOOK* s1 = (BOOK*)e1;          // 指针强制转换
    BOOK* s2 = (BOOK*)e2;
    return s1->price - s2->price;
}
int main()
{
    int n = 0;
    scanf("%d", &n);
    BOOK arr[50];
    for (int i = 0; i < n; i++)
    {
        scanf("%s %d", &arr[i].name, &arr[i].price);
    }
    int sz = sizeof(arr) / sizeof(arr[0]);
    qsort(arr, n, sizeof(arr[0]), cmp_by_price);//快排
    for (int i = 0; i < n; i++)
    {
        printf("%s\n", arr[i].name);
    }
    return 0;
}
```
1. **成绩排序**  
    多级排序：总分→语文→数学→姓名
```C    
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
typedef struct x
{
	char name[11]; //姓名
	int x;      //语文
	int y;      //数学
	int z;      //英语
	int score;   //总分
}book;
int cmp_char(const void* e1, const void* e2)
{
	book* s1 = (book*)e1;
	book* s2 = (book*)e2;
	if (s1->score != s2->score)  //按总成绩排序
	{
		return s2->score - s1->score;
	}
	else
	{
		if (s1->x != s2->x)  //按语文成绩排序
		{
			return s2->x - s1->x;
		}
		else
		{
			if (s1->y != s2->y) //按数学成绩排序
			{
				return s2->y - s1->y;
			}
			else
			{
				return strcmp(s1->name, s2->name);  // 按姓名的字典顺序排序
			}
		}
	}
}
int main()
{
	int n;
	scanf("%d", &n);
	book arr[1001];
	for (int i = 0; i < n; i++)
	{
		scanf("%s %d %d %d", &arr[i].name, &arr[i].x, &arr[i].y, &arr[i].z);
		arr[i].score = arr[i].x + arr[i].y + arr[i].z;
	}
	qsort(arr, n, sizeof(arr[0]), cmp_char);
	for (int i = 0; i < n; i++)
	{
		printf("%s %d %d %d\n", arr[i].name, arr[i].x, arr[i].y, arr[i].z);
	}
	return 0;
}
```
## 💡 关键点总结

1. 结构体是自定义的复合数据类型，可以包含不同类型的成员
2. 结构体变量使用`.`访问成员，结构体指针使用`->`
3. 结构体指针需要特别注意内存分配问题
4. typedef可以简化结构体类型的使用
5. 结构体在数据结构和复杂数据管理中非常有用

## 🚀 学习建议

1. 多练习结构体定义和初始化的各种写法
2. 重点掌握结构体指针的使用
3. 通过实际编程题目巩固知识
4. 理解结构体在数据结构中的基础作用

这篇博客对C语言结构体进行了全面而深入的讲解，配合丰富的代码示例，是学习结构体的优秀资源。建议读者按照文章顺序逐步学习，并通过练习题目检验学习效果。